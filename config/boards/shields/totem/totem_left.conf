# Left half is now the split peripheral (trackpoint + central role moved
# to the right half). No pointing / I2C / USB-logging configs needed
# here; this file is kept as a placeholder so the shield system still
# picks up totem_left as a valid build target.
